/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['admin.mirror.org.in'],
    //domains: ['localhost'],

  },
}



module.exports = nextConfig
